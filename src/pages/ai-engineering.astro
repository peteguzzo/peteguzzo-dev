---
import BaseLayout from '../layouts/BaseLayout.astro';
import CTA from '../components/CTA.astro';
---

<BaseLayout
  title="AI Engineering + Operational Intelligence"
  description="Pete Guzzo designs AI systems where agents maintain institutional memory, verify before executing, and remain governed by a human-controlled supervisory layer — so the work gets smarter over time, not just faster."
>

  <!-- ─────────────────────────────────────────
       SECTION 1: LOCKED OPENING — COBALT BLUE
       Copy locked by Pete via CLM — 2026-08-12
       ───────────────────────────────────────── -->
  <section class="ai-opening ai-bg-cobalt" style="background:#1B4FD8;">
    <div class="max-w-4xl mx-auto px-6 py-24 md:py-36">
      <p class="ai-eyebrow">AI Engineering &amp; Operational Intelligence</p>
      <h1 class="ai-h1 font-display mt-6">
        This page was built<br class="hidden md:block" /> by the system it describes.
      </h1>
      <div class="ai-intro mt-10 max-w-2xl space-y-5">
        <p>
          CORTEX is the operational intelligence system I've been engineering around
          modern AI agents. It gives specialized agents persistent context, defined
          responsibilities, supervisory oversight, verification, learning and human
          approval gates — so they can work together without every new session
          starting from zero.
        </p>
        <p>This page became one of its own projects.</p>
        <p>
          Research, source verification, content architecture, writing, engineering
          and QA were distributed across specialized agents under CLM supervision.
          The work was reconciled against prior project history, verified before
          implementation, and kept offline until I approved the result.
        </p>
      </div>
      <p class="ai-pivot mt-14">
        AI agents are powerful, but most of them still walk into the room like it's their first day.
      </p>
    </div>
    <!-- Production context — on set at Shear Media Studios -->
    <div class="ai-photo-strip">
      <img
        src="/images/about/pete-on-set-shear.jpg"
        alt="Pete Guzzo on set at Shear Media Studios"
        class="ai-photo-strip-img"
        loading="eager"
      />
      <div class="ai-photo-strip-scrim"></div>
    </div>
  </section>

  <!-- ─────────────────────────────────────────
       SECTION 2: THE FOUR LEVELS — DEEP NAVY
       Pure concept — no external data required
       ───────────────────────────────────────── -->
  <section class="ai-bg-navy" style="background:#0B111F;">
    <div class="max-w-4xl mx-auto px-6 py-4">

      <!-- Level 1 -->
      <div class="ai-level ai-level-1">
        <div class="ai-level-marker">
          <span class="ai-level-num">01</span>
          <span class="ai-level-name">Manual</span>
        </div>
        <div class="ai-level-body">
          <p class="ai-level-lead">You give the agent a task. It executes. It forgets.</p>
          <p class="ai-level-sub">
            The next session starts exactly where the first one did — at zero. If
            last Tuesday's bug taught you something, you're the one carrying that
            lesson. The agent isn't. This is where most AI-assisted work lives.
          </p>
        </div>
      </div>

      <!-- Level 2 -->
      <div class="ai-level ai-level-2">
        <div class="ai-level-marker">
          <span class="ai-level-num">02</span>
          <span class="ai-level-name">Documentation</span>
        </div>
        <div class="ai-level-body">
          <p class="ai-level-lead">You write it down. A README. A setup doc. A prompt template.</p>
          <p class="ai-level-sub">
            Better. But documentation captures what you remembered to write, not
            what actually happened. It doesn't record the three things you tried
            before finding the one that worked. It tells the agent what you planned.
            It doesn't tell the agent what the project learned.
          </p>
        </div>
      </div>

      <!-- Level 3 -->
      <div class="ai-level ai-level-3">
        <div class="ai-level-marker">
          <span class="ai-level-num">03</span>
          <span class="ai-level-name">Handoff Notes</span>
        </div>
        <div class="ai-level-body">
          <p class="ai-level-lead">Before the session ends, you write down where you left off.</p>
          <p class="ai-level-sub">
            Real progress. But the handoff is only as good as what you remembered
            to include. The notes age. They go stale. They're written by a human
            who is also doing the work — which means they're incomplete by default.
          </p>
        </div>
      </div>

      <!-- Level 4 — gear shift -->
      <div class="ai-level ai-level-4">
        <div class="ai-level-marker">
          <span class="ai-level-num">04</span>
          <span class="ai-level-name">CORTEX</span>
        </div>
        <div class="ai-level-body">
          <p class="ai-level-4-lead">The system that stays.</p>
          <p class="ai-level-4-sub">
            CORTEX operates as an experienced supervisor who never leaves the
            project. It accumulates verified experience across every session — what
            broke, what the last agent tried, what was confirmed to work, and why.
            The next agent doesn't start from zero. It starts from a verified
            operational baseline.
          </p>
          <p class="ai-level-4-sub mt-4">
            Reflex rules are earned from real incidents, not written from theory.
            Session handoffs are structured, not improvised. Memory files are
            human-readable, editable, and scoped per project. If a lesson is wrong,
            you edit the file. No retraining required.
          </p>
          <blockquote class="ai-pull-quote mt-8">
            "The model's weights are unchanged. The model's effective behavior
            improves because every new session starts with a richer, verified
            operational context."
          </blockquote>
          <p class="ai-pull-attr mt-3">CORTEX Project Journal</p>
        </div>
      </div>

    </div>
  </section>

  <!-- ─────────────────────────────────────────
       SECTION 3: META-STORY — WARM CREAM
       This page is one of the case studies
       ───────────────────────────────────────── -->
  <section class="ai-bg-cream ai-section-cream" style="background:#F5F3EE;color:#111827;">
    <div class="max-w-4xl mx-auto px-6 py-20">
      <p class="ai-eyebrow">How this page was made</p>
      <h2 class="font-display text-2xl md:text-3xl mt-4 ai-cream-heading">
        This page is one of the case studies.
      </h2>
      <p class="mt-5 max-w-2xl leading-relaxed ai-cream-sub">
        It didn't get handed to a single agent with a blank prompt. It went through
        the same operational system it's describing.
      </p>

      <!-- Production context image — live control room wall -->
      <div class="ai-photo-feature mt-10 mb-12">
        <img
          src="/images/studios/gallery/control-room-live-wall.jpg"
          alt="Live broadcast control room wall at Shear Media Studios"
          class="ai-photo-feature-img"
          loading="lazy"
        />
        <p class="ai-photo-caption">Live wall and control room infrastructure — Shear Media Studios, Tampa</p>
      </div>

      <div class="ai-chain mt-4 space-y-0">

        <div class="ai-chain-row">
          <div class="ai-chain-role">CLM supervision</div>
          <div class="ai-chain-what">
            Mission defined. Project context retained across all sessions. Work reconciled
            against prior project history — including three prior field agents who had
            already audited peteguzzo.com — before a single word of copy was written.
          </div>
        </div>

        <div class="ai-chain-row">
          <div class="ai-chain-role">Source recon</div>
          <div class="ai-chain-what">
            CORTEX system files, peteguzzo.com docs, Heights Lounge, Shear Media Studios
            SEO, Chicago SRT operational learning, and 100+ image assets read and mapped.
            No claim entered the draft without a verified source.
          </div>
        </div>

        <div class="ai-chain-row">
          <div class="ai-chain-role">Claims verification</div>
          <div class="ai-chain-what">
            Every factual claim mapped to a source file before copy was written.
            Prohibited language enforced at the sentence level. Revenue figures held
            until Pete's confirmation — not guessed, not approximated.
          </div>
        </div>

        <div class="ai-chain-row">
          <div class="ai-chain-role">Architecture</div>
          <div class="ai-chain-what">
            Page section structure, visual direction, SEO metadata, and internal link
            plan designed and reviewed before copy began.
          </div>
        </div>

        <div class="ai-chain-row">
          <div class="ai-chain-role">Copy</div>
          <div class="ai-chain-what">
            Written against the verified claims map. Language constraints enforced:
            no overclaiming autonomy, no framing that disappears humans from the process.
          </div>
        </div>

        <div class="ai-chain-row">
          <div class="ai-chain-role">Engineering</div>
          <div class="ai-chain-what">
            Built in the same Astro + Cloudflare Pages stack the page describes.
          </div>
        </div>

        <div class="ai-chain-row">
          <div class="ai-chain-role">QA</div>
          <div class="ai-chain-what">
            Launch checklist execution, link verification, claims cross-check before commit.
          </div>
        </div>

        <div class="ai-chain-row ai-chain-row--pete">
          <div class="ai-chain-role">Pete</div>
          <div class="ai-chain-what">
            Locked the opening. Set the framing rules. Approved the direction.
            Launch authority. Not a formality.
          </div>
        </div>

      </div>

      <p class="ai-meta-note mt-12 max-w-2xl leading-relaxed ai-cream-muted">
        The impressive part isn't that humans disappeared. The impressive part is that
        multiple specialized agents could work over time, preserve operational learning,
        hand work to one another, and remain governed by a human-controlled supervisory
        layer throughout. That's the system. This page is what it produces.
      </p>
    </div>
  </section>

  <!-- ─────────────────────────────────────────
       SECTION 4: CASE STUDIES — DEEP NAVY
       Real projects. Real engineering.
       ───────────────────────────────────────── -->
  <section class="ai-bg-navy" style="background:#0B111F;">
    <div class="max-w-4xl mx-auto px-6 py-20">
      <p class="ai-eyebrow">The work</p>
      <h2 class="font-display text-2xl md:text-3xl mt-4 text-paper">Real projects. Real engineering.</h2>
      <p class="mt-4 ai-muted-60">These are not demos. Each one is a deployed system with a documented operational record.</p>

      <div class="mt-16 space-y-20">

        <!-- Case Study A: peteguzzo.com -->
        <div class="ai-cs">
          <div class="ai-cs-header">
            <span class="ai-cs-num">A</span>
            <div>
              <p class="ai-cs-label">Operational Intelligence</p>
              <h3 class="ai-cs-title font-display">peteguzzo.com</h3>
            </div>
          </div>
          <div class="ai-cs-grid mt-6">
            <div class="ai-cs-block">
              <p class="ai-cs-block-label">The problem</p>
              <p class="ai-cs-block-text">
                73 pages. 50 work items. 10 articles. Three prior field agents had already
                conducted audits. None of them made a single production change to the live site.
                All findings went through CLM before any implementation was authorized.
              </p>
            </div>
            <div class="ai-cs-block">
              <p class="ai-cs-block-label">The engineering</p>
              <p class="ai-cs-block-text">
                Soft 404 problem discovered: every unmatched URL returning HTTP 200. AI/LLM
                discoverability assessment: AI could answer "Who is Pete Guzzo?" but only
                generically — the specific facts weren't structured for machine consumption.
                Five authority pillars defined. Full implementation plan written as
                self-contained, delegatable task bundles.
              </p>
            </div>
            <div class="ai-cs-block ai-cs-block--full">
              <p class="ai-cs-block-label">What changed</p>
              <p class="ai-cs-block-text">
                A complete roadmap with zero production changes. Every recommendation
                traceable to a verified finding. The next agent that runs the implementation
                has a specific, bounded task — not a blank page.
              </p>
            </div>
          </div>
        </div>

        <!-- Case Study B: Heights Lounge -->
        <div class="ai-cs">
          <div class="ai-cs-header">
            <span class="ai-cs-num">B</span>
            <div>
              <p class="ai-cs-label">Production Engineering</p>
              <h3 class="ai-cs-title font-display">Heights Lounge CMS</h3>
            </div>
          </div>
          <div class="ai-cs-grid mt-6">
            <div class="ai-cs-block">
              <p class="ai-cs-block-label">The problem</p>
              <p class="ai-cs-block-text">
                A Tampa kava bar needed to manage their own website: events, schedules,
                brand colors, content. The engineering stack — Next.js, TinaCMS,
                Cloudflare Pages, an AI auto-fill worker — had to be completely invisible
                to the client.
              </p>
            </div>
            <div class="ai-cs-block">
              <p class="ai-cs-block-label">The engineering</p>
              <p class="ai-cs-block-text">
                14 operational rules earned from real incidents — not written from theory.
                The tina-lock.json 3-key sync pattern: learned from two separate schema
                mismatch incidents, now a reflex rule. Boolean toggle bug: documented,
                workaround applied, not rediscovered. SEO removed from CMS after causing
                the original mismatch — reflex rule: do not add it back. AI auto-fill via
                Cloudflare Worker. Auto-deploy: push to GitHub → Cloudflare rebuilds.
              </p>
            </div>
            <div class="ai-cs-block ai-cs-block--full">
              <p class="ai-cs-block-label">What changed</p>
              <p class="ai-cs-block-text">
                Complexity in engineering, simplicity for the client. The 14 earned rules
                mean the next engineering session doesn't pay for any of those discoveries again.
              </p>
            </div>
          </div>
        </div>

        <!-- Case Study C: SEO + AI Discovery -->
        <div class="ai-cs">
          <div class="ai-cs-header">
            <span class="ai-cs-num">C</span>
            <div>
              <p class="ai-cs-label">SEO + AI Discoverability</p>
              <h3 class="ai-cs-title font-display">peteguzzo.com + Shear Media Studios</h3>
            </div>
          </div>
          <div class="ai-cs-grid mt-6">
            <div class="ai-cs-block">
              <p class="ai-cs-block-label">The problem</p>
              <p class="ai-cs-block-text">
                AI systems could describe Pete correctly but generically. The specific facts
                that distinguish him weren't structured for machine consumption. At Shear
                Media Studios, 20 SEO articles needed systematic development with correct
                entity architecture and structured data.
              </p>
            </div>
            <div class="ai-cs-block">
              <p class="ai-cs-block-label">The engineering</p>
              <p class="ai-cs-block-text">
                5-pillar authority framework. Every page mapped to an authority pillar
                before copy. llms.txt gap analysis. Person schema gap analysis. Seven
                specific AI discoverability questions answered from structured files.
                For Shear: three-file source structure per article (.md + .json +
                .schema.json). When Squarespace clipboard paste failed — two-strike
                confirmed — ClipboardEvent dispatch developed and implemented: bypasses
                the system clipboard entirely.
              </p>
            </div>
            <div class="ai-cs-block ai-cs-block--full">
              <p class="ai-cs-block-label">What changed</p>
              <p class="ai-cs-block-text">
                AI discoverability treated as a design constraint from the start — not an
                afterthought. A methodology that scales: same three-file structure and
                claims verification protocol across 20 articles.
              </p>
            </div>
          </div>
        </div>

        <!-- Case Study D: SRT/Network -->
        <div class="ai-cs">
          <div class="ai-cs-header">
            <span class="ai-cs-num">D</span>
            <div>
              <p class="ai-cs-label">Supervised Infrastructure Engineering</p>
              <h3 class="ai-cs-title font-display">Live Broadcast Connectivity Test</h3>
            </div>
          </div>
          <div class="ai-cs-grid mt-6">
            <div class="ai-cs-block">
              <p class="ai-cs-block-label">The problem</p>
              <p class="ai-cs-block-text">
                A multi-party live broadcast test spanning three separate control domains.
                Multiple experienced engineers asserted the required configuration was
                impossible through a specific network path. The team did not accept
                this as a verified constraint.
              </p>
            </div>
            <div class="ai-cs-block">
              <p class="ai-cs-block-label">The engineering</p>
              <p class="ai-cs-block-text">
                Full topology reconnaissance before any change. Device address stabilized
                before downstream dependencies were created. A single narrowly scoped
                change — nothing adjacent touched. The engineers' assertion was classified:
                VERIFIED (tested against this system)? DOCUMENTED (published by a
                controlling authority)? Or ASSUMED from prior practice? It was ASSUMED.
                Tested with the smallest reversible experiment the topology allowed.
              </p>
            </div>
            <div class="ai-cs-block ai-cs-block--full">
              <p class="ai-cs-block-label">What changed</p>
              <p class="ai-cs-block-text">
                All feeds and the return path succeeded. Expert opinion was treated as
                valuable evidence, but not as a substitute for verified system behavior.
                The constraint classification framework captured as a CORTEX reflex.
              </p>
            </div>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ─────────────────────────────────────────
       SECTION 5: ARCHITECTURE — DEEP COBALT
       Verified from TENTACLES_V0.md
       ───────────────────────────────────────── -->
  <section class="ai-bg-cobalt-deep" style="background:#1340B0;">
    <div class="max-w-4xl mx-auto px-6 py-20">
      <p class="ai-eyebrow">Architecture</p>
      <h2 class="font-display text-2xl md:text-3xl mt-4 text-paper">
        A supervisory network — not a chatbot.
      </h2>
      <p class="mt-4 ai-muted-60 max-w-2xl leading-relaxed">
        CORTEX operates as a Collective Learning Mainframe: a persistent supervisory
        layer that synthesizes field-session experience into verified operational
        doctrine, governs agent behavior within defined boundaries, and maintains
        institutional knowledge across every project. It is not autonomous. It is supervised.
      </p>

      <!-- Control room — operational infrastructure in practice -->
      <div class="ai-photo-arch mt-10 mb-4">
        <img
          src="/images/studios/Shear-Media-Studios-Control-Room.jpg"
          alt="Operations and broadcast control room at Shear Media Studios"
          class="ai-photo-arch-img"
          loading="lazy"
        />
        <p class="ai-photo-caption ai-photo-caption--light">Control room infrastructure — the physical layer beneath the operational intelligence layer.</p>
      </div>

      <!-- Architecture diagram — styled HTML, no images, no external libraries -->
      <div class="ai-arch mt-10">

        <div class="ai-arch-node ai-arch-node--clm">
          <p class="ai-arch-node-label">CORTEX / CLM</p>
          <p class="ai-arch-node-desc">Governance · Memory · Learning · Approval</p>
          <p class="ai-arch-node-detail">
            The supervisory layer. Retains project context. Synthesizes lessons from all
            field sessions. Defines agent boundaries. Holds human approval gates.
          </p>
        </div>

        <div class="ai-arch-arrow-down">
          <span class="ai-arch-arrow-label">Task bundles · Directives · Governance</span>
          <div class="ai-arch-arrow-line"></div>
          <span class="ai-arch-arrow-label ai-arch-arrow-label--up">Status · Candidate lessons · Escalations</span>
        </div>

        <div class="ai-arch-row">
          <div class="ai-arch-node ai-arch-node--agent">
            <p class="ai-arch-node-label">Field Agents (Claude Code)</p>
            <p class="ai-arch-node-desc">Browser · CMS · Execution</p>
            <p class="ai-arch-node-detail">
              Specialized agents assigned to specific tasks. Each operates inside a
              defined scope. Findings reported to CLM at verified checkpoints.
            </p>
          </div>
          <div class="ai-arch-connector"></div>
          <div class="ai-arch-node ai-arch-node--agent">
            <p class="ai-arch-node-label">Codex</p>
            <p class="ai-arch-node-desc">Code · Schema · Architecture</p>
            <p class="ai-arch-node-detail">
              Technical specialist. Schema design, structured data, technical SEO,
              repo inspection. Registered August 2026.
            </p>
          </div>
        </div>

        <div class="ai-arch-note mt-10">
          <p class="text-paper/50 text-sm">
            <span class="text-paper/70 font-medium">TENTACLES V0</span> — current operating model.
            Manual checkpoint file exchange. Sessions write structured handoffs; CLM reads at defined
            checkpoints. Not a live network connection — a disciplined operational protocol.
          </p>
          <p class="text-paper/40 text-sm mt-2">
            TENTACLES V1 (heartbeat infrastructure) and Ruflo (orchestration) are designed but not yet active.
          </p>
        </div>

      </div>

      <!-- Learning loop -->
      <div class="ai-loop mt-14">
        <p class="ai-eyebrow mb-4">The learning loop</p>
        <div class="ai-loop-steps">
          <div class="ai-loop-step">
            <span class="ai-loop-step-num">1</span>
            <p class="ai-loop-step-text">Field agents encounter real conditions and document findings.</p>
          </div>
          <div class="ai-loop-arrow">→</div>
          <div class="ai-loop-step">
            <span class="ai-loop-step-num">2</span>
            <p class="ai-loop-step-text">Lessons meeting the evidence standard flow to CLM at checkpoints.</p>
          </div>
          <div class="ai-loop-arrow">→</div>
          <div class="ai-loop-step">
            <span class="ai-loop-step-num">3</span>
            <p class="ai-loop-step-text">CLM synthesizes. Verified lessons promote to canonical doctrine.</p>
          </div>
          <div class="ai-loop-arrow">→</div>
          <div class="ai-loop-step">
            <span class="ai-loop-step-num">4</span>
            <p class="ai-loop-step-text">Next agent starts with accumulated context — not from zero.</p>
          </div>
        </div>
      </div>

    </div>
  </section>

  <!-- ─────────────────────────────────────────
       SECTION 6: PHILOSOPHY — WARM CREAM
       Philosophy framing approved as direction
       ───────────────────────────────────────── -->
  <section class="ai-bg-cream ai-section-cream" style="background:#F5F3EE;color:#111827;">
    <div class="max-w-4xl mx-auto px-6 py-20">
      <p class="ai-eyebrow">How it actually works</p>
      <h2 class="font-display text-2xl md:text-3xl mt-4 ai-cream-heading">
        External memory beats internal training<br class="hidden md:block" /> — for this class of problem.
      </h2>

      <div class="ai-philosophy mt-10 space-y-6 max-w-2xl">
        <p class="ai-cream-body leading-relaxed">
          Machine learning improves by changing the model. Retraining. Weight updates.
          The learning is inside the system, which means it's opaque, expensive to correct,
          and impossible to scope per project.
        </p>
        <p class="ai-cream-body leading-relaxed">
          CORTEX improves by changing the context. External state files. Verified incident
          records. Reflex rules earned from real failures. Every lesson is human-readable,
          editable, and scoped to the project that generated it.
        </p>
      </div>

      <!-- Comparison table -->
      <div class="ai-compare mt-14 overflow-x-auto">
        <table class="ai-compare-table w-full text-sm">
          <thead>
            <tr>
              <th class="ai-compare-th ai-compare-th--dim">Dimension</th>
              <th class="ai-compare-th">Machine learning</th>
              <th class="ai-compare-th ai-compare-th--highlight">Persistent Operational Learning</th>
            </tr>
          </thead>
          <tbody>
            {[
              ['Learning mechanism', 'Weight updates via training', 'External state files'],
              ['Memory location', 'Inside the model', 'Human-readable files'],
              ['Auditability', 'Opaque', 'Fully visible'],
              ['Editability', 'Requires retraining', 'Edit a text file'],
              ['Project isolation', 'Not possible', 'Memory scoped per project'],
              ['Error correction', 'Requires retraining', 'Delete or update the rule'],
              ['Evidence requirement', 'Statistical patterns', 'Incident-specific, traceable'],
              ['Explainability', 'Limited', 'Every action traceable to source'],
            ].map(([dim, ml, pol]) => (
              <tr class="ai-compare-row">
                <td class="ai-compare-td ai-compare-td--dim">{dim}</td>
                <td class="ai-compare-td">{ml}</td>
                <td class="ai-compare-td ai-compare-td--highlight">{pol}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      <div class="ai-philosophy-close mt-14 max-w-2xl space-y-5">
        <p class="ai-cream-body leading-relaxed">
          AI that learns through retraining is a black box. The learning happened. You can
          observe the changed behavior. You cannot read the lesson, correct it, or scope
          it to one project without affecting all the others.
        </p>
        <p class="ai-cream-body leading-relaxed">
          AI that learns through accumulated external memory is an audit trail. The lesson
          is a text file. You can read it. You can edit it. You can delete it if it was
          wrong. You can see exactly which incident generated it, what the evidence was,
          and who confirmed it.
        </p>
        <p class="ai-principle font-display text-xl md:text-2xl leading-snug mt-8">
          The organization should not have to pay for the same mistake twice.
        </p>
      </div>
    </div>
  </section>

  <!-- ─────────────────────────────────────────
       SECTION 7: SERVICES — COBALT BLUE
       ───────────────────────────────────────── -->
  <section class="ai-bg-cobalt" style="background:#1B4FD8;">
    <div class="max-w-4xl mx-auto px-6 py-20">
      <h2 class="font-display text-2xl md:text-3xl text-paper">What I design and build</h2>
      <p class="mt-4 ai-muted-60 max-w-2xl leading-relaxed">
        This is not "I use AI to work faster." This is the architecture of how AI-assisted
        work gets structured, governed, and made reliable over time.
      </p>

      <div class="ai-services mt-12 grid md:grid-cols-2 gap-8">

        <div class="ai-service">
          <p class="ai-service-title">Operational intelligence infrastructure</p>
          <p class="ai-service-body">
            The memory layer: state files, session handoffs, reflex rules, incident logs —
            structured so knowledge accumulates instead of evaporating. Designed per project,
            not applied as a generic template.
          </p>
        </div>

        <div class="ai-service">
          <p class="ai-service-title">Multi-agent coordination</p>
          <p class="ai-service-body">
            Field agents with defined scopes. Technical agents with specific capabilities.
            Routing logic that assigns tasks to the right agent. Governance gates that hold
            the right decisions for human approval.
          </p>
        </div>

        <div class="ai-service">
          <p class="ai-service-title">AI-assisted workflow integration</p>
          <p class="ai-service-body">
            Not "use Claude for this task" — but "design the system that uses Claude
            correctly for this class of task, with the right oversight and verification
            checkpoints." The workflow, not the prompt.
          </p>
        </div>

        <div class="ai-service">
          <p class="ai-service-title">Supervised autonomy design</p>
          <p class="ai-service-body">
            What agents can do autonomously. What requires a recommendation and human
            decision. What requires explicit human approval before any action is taken.
            The governance layer that makes AI work trustworthy — not just capable.
          </p>
        </div>

      </div>

      <div class="mt-12">
        <CTA href="/contact">Get in touch →</CTA>
      </div>
    </div>
  </section>

  <!-- ─────────────────────────────────────────
       SECTION 8: RELATED READING — DEEP NAVY
       ───────────────────────────────────────── -->
  <section class="ai-bg-navy" style="background:#0B111F;">
    <div class="max-w-4xl mx-auto px-6 py-16">
      <p class="ai-eyebrow mb-8">Read more</p>
      <div class="space-y-5">
        <a href="/industry-insights/how-ai-actually-helps-a-production-team-move-faster" class="ai-related-link group block">
          <span class="ai-related-arrow text-paper/30 group-hover:text-paper/70 transition-colors">→</span>
          <span class="ai-related-text">How AI Actually Helps a Production Team Move Faster</span>
          <span class="ai-related-sub">The production-side view: practical AI integration without the hype.</span>
        </a>
        <a href="/shear-enterprises" class="ai-related-link group block">
          <span class="ai-related-arrow text-paper/30 group-hover:text-paper/70 transition-colors">→</span>
          <span class="ai-related-text">EVP, Shear Enterprises</span>
          <span class="ai-related-sub">AI-assisted operations across a live commerce and studio operation.</span>
        </a>
        <a href="/studios" class="ai-related-link group block">
          <span class="ai-related-arrow text-paper/30 group-hover:text-paper/70 transition-colors">→</span>
          <span class="ai-related-text">Studios Are Operating Systems</span>
          <span class="ai-related-sub">The physical infrastructure side of the same operational philosophy.</span>
        </a>
        <a href="/contact" class="ai-related-link group block">
          <span class="ai-related-arrow text-paper/30 group-hover:text-paper/70 transition-colors">→</span>
          <span class="ai-related-text">Get in touch</span>
          <span class="ai-related-sub">Start a conversation.</span>
        </a>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  /* ─── Palette tokens — cobalt system ─── */
  :root {
    --cobalt:        #1B4FD8;
    --cobalt-deep:   #1340B0;
    --cobalt-hi:     #4e8ef7;   /* readable on dark navy bg */
    --cobalt-light:  #93c5fd;   /* readable on cobalt bg */
    --navy:          #0B111F;
    --cream:         #F5F3EE;
    --cream-border:  rgba(17, 24, 39, 0.12);
    --cream-text:    #111827;
    --cream-sub:     #4a5568;
    --cream-muted:   #718096;
    --ai-signal:     #6ea8fe;
    --ai-signal-dim: rgba(110, 168, 254, 0.15);
  }

  /* ─── Section backgrounds ─── */
  .ai-bg-cobalt      { background: var(--cobalt); }
  .ai-bg-cobalt-deep { background: var(--cobalt-deep); }
  .ai-bg-navy        { background: var(--navy); }
  .ai-bg-cream       { background: var(--cream); }

  /* ─── Photo elements ─── */
  .ai-photo-strip {
    position: relative;
    overflow: hidden;
    height: clamp(12rem, 30vw, 22rem);
  }

  .ai-photo-strip-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 30%;
    display: block;
  }

  .ai-photo-strip-scrim {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to bottom,
      rgba(27, 79, 216, 0.55) 0%,
      rgba(27, 79, 216, 0.10) 40%,
      rgba(11, 17, 31, 0.60) 100%
    );
  }

  .ai-photo-feature {
    border-radius: 6px;
    overflow: hidden;
  }

  .ai-photo-feature-img {
    width: 100%;
    height: clamp(14rem, 35vw, 24rem);
    object-fit: cover;
    object-position: center;
    display: block;
  }

  .ai-photo-arch {
    border-radius: 6px;
    overflow: hidden;
  }

  .ai-photo-arch-img {
    width: 100%;
    height: clamp(12rem, 28vw, 20rem);
    object-fit: cover;
    object-position: center 25%;
    display: block;
  }

  .ai-photo-caption {
    font-size: 0.75rem;
    letter-spacing: 0.04em;
    color: var(--cream-muted);
    margin-top: 0.625rem;
    line-height: 1.5;
  }

  .ai-photo-caption--light {
    color: rgba(250, 249, 246, 0.40);
  }

  /* ─── Eyebrow — shared ─── */
  .ai-eyebrow {
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.16em;
    color: rgba(250, 249, 246, 0.38);
    font-weight: 600;
  }

  /* ─── SECTION 1: Opening (cobalt) ─── */
  .ai-h1 {
    font-size: clamp(2rem, 5vw, 3.5rem);
    line-height: 1.1;
    letter-spacing: -0.025em;
    color: #ffffff;
  }

  .ai-intro p {
    color: rgba(255, 255, 255, 0.80);
    line-height: 1.75;
    font-size: 1.0625rem;
  }

  .ai-pivot {
    font-family: var(--font-serif);
    font-weight: 600;
    font-size: clamp(1.25rem, 3vw, 1.75rem);
    color: #ffffff;
    letter-spacing: -0.01em;
    line-height: 1.3;
    padding-top: 2rem;
    border-top: 1px solid rgba(255, 255, 255, 0.20);
  }

  /* ─── SECTION 2: Four Levels (navy) ─── */
  .ai-level {
    display: grid;
    grid-template-columns: 10rem 1fr;
    gap: 2rem;
    padding: 3.5rem 0;
    border-bottom: 1px solid rgba(240, 244, 255, 0.08);
  }

  .ai-level-marker {
    padding-top: 0.125rem;
  }

  .ai-level-num {
    display: block;
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: rgba(240, 244, 255, 0.25);
    font-weight: 600;
    margin-bottom: 0.5rem;
  }

  .ai-level-name {
    display: block;
    font-family: var(--font-serif);
    font-size: 0.9rem;
    color: rgba(240, 244, 255, 0.40);
    letter-spacing: -0.01em;
  }

  /* Level visual weight progression */
  .ai-level-1 { opacity: 0.55; }
  .ai-level-2 { opacity: 0.70; }
  .ai-level-3 { opacity: 0.85; }
  .ai-level-4 {
    opacity: 1;
    border-left: 2px solid var(--cobalt-hi);
    padding-left: 1.5rem;
    margin-left: -1.5rem;
    border-bottom-color: transparent;
  }

  .ai-level-lead {
    font-family: var(--font-serif);
    font-size: 1.125rem;
    font-weight: 600;
    color: rgba(240, 244, 255, 0.85);
    letter-spacing: -0.01em;
    line-height: 1.4;
    margin-bottom: 0.75rem;
  }

  .ai-level-sub {
    font-size: 0.9375rem;
    color: rgba(240, 244, 255, 0.55);
    line-height: 1.7;
  }

  /* Level 4 distinction */
  .ai-level-4 .ai-level-num {
    color: var(--cobalt-hi);
  }

  .ai-level-4 .ai-level-name {
    color: rgba(240, 244, 255, 0.80);
    font-size: 1rem;
  }

  .ai-level-4-lead {
    font-family: var(--font-serif);
    font-size: clamp(1.25rem, 3vw, 1.6rem);
    font-weight: 600;
    color: #f0f4ff;
    letter-spacing: -0.02em;
    line-height: 1.3;
    margin-bottom: 1rem;
  }

  .ai-level-4-sub {
    font-size: 0.9375rem;
    color: rgba(240, 244, 255, 0.72);
    line-height: 1.75;
  }

  .ai-pull-quote {
    border-left: none;
    margin: 0;
    padding: 1.25rem 1.5rem;
    background: rgba(78, 142, 247, 0.12);
    border-radius: 4px;
    font-family: var(--font-serif);
    font-size: 1rem;
    font-style: italic;
    color: rgba(240, 244, 255, 0.85);
    line-height: 1.6;
  }

  .ai-pull-attr {
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: rgba(240, 244, 255, 0.25);
    font-weight: 600;
  }

  /* ─── SECTION 3: Meta-story (cream) — scoped overrides ─── */
  .ai-section-cream .ai-eyebrow {
    color: rgba(17, 24, 39, 0.42);
  }

  .ai-cream-heading {
    color: var(--cream-text);
  }

  .ai-cream-sub {
    color: var(--cream-sub);
  }

  .ai-cream-body {
    color: var(--cream-sub);
  }

  .ai-cream-muted {
    color: var(--cream-muted);
  }

  .ai-section-cream .ai-chain {
    border-top: 1px solid var(--cream-border);
  }

  .ai-chain {
    border-top: 1px solid rgba(250, 249, 246, 0.10);
  }

  .ai-chain-row {
    display: grid;
    grid-template-columns: 10rem 1fr;
    gap: 2rem;
    padding: 1.25rem 0;
    border-bottom: 1px solid rgba(250, 249, 246, 0.06);
    align-items: baseline;
  }

  .ai-section-cream .ai-chain-row {
    border-bottom: 1px solid rgba(17, 24, 39, 0.08);
  }

  .ai-chain-row--pete {
    border-bottom: none;
    padding-top: 1.5rem;
  }

  .ai-chain-role {
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: rgba(250, 249, 246, 0.35);
    font-weight: 600;
    padding-top: 0.125rem;
  }

  .ai-section-cream .ai-chain-role {
    color: rgba(17, 24, 39, 0.38);
  }

  .ai-chain-row--pete .ai-chain-role {
    color: var(--cobalt);
  }

  .ai-chain-what {
    font-size: 0.9rem;
    color: rgba(250, 249, 246, 0.65);
    line-height: 1.65;
  }

  .ai-section-cream .ai-chain-what {
    color: var(--cream-sub);
  }

  .ai-chain-row--pete .ai-chain-what {
    color: rgba(250, 249, 246, 0.85);
  }

  .ai-section-cream .ai-chain-row--pete .ai-chain-what {
    color: var(--cream-text);
  }

  .ai-meta-note {
    font-size: 0.9375rem;
    line-height: 1.75;
  }

  /* ─── SECTION 4 + 8: Case Studies / Related (navy) ─── */
  .ai-muted-60 {
    color: rgba(240, 244, 255, 0.60);
  }

  .ai-cs-header {
    display: flex;
    align-items: flex-start;
    gap: 1.25rem;
  }

  .ai-cs-num {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.25rem;
    height: 2.25rem;
    border-radius: 50%;
    border: 1px solid rgba(240, 244, 255, 0.18);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.06em;
    color: rgba(240, 244, 255, 0.45);
    flex-shrink: 0;
    margin-top: 0.125rem;
  }

  .ai-cs-label {
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: rgba(240, 244, 255, 0.35);
    font-weight: 600;
    margin-bottom: 0.375rem;
  }

  .ai-cs-title {
    font-size: 1.25rem;
    color: #f0f4ff;
    letter-spacing: -0.02em;
    line-height: 1.3;
  }

  .ai-cs-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem 3rem;
    padding-left: 3.5rem;
    border-top: 1px solid rgba(240, 244, 255, 0.08);
    padding-top: 1.5rem;
  }

  .ai-cs-block--full {
    grid-column: 1 / -1;
    padding-top: 1rem;
    border-top: 1px solid rgba(240, 244, 255, 0.06);
    margin-top: 0.5rem;
  }

  .ai-cs-block-label {
    font-size: 9.5px;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: rgba(240, 244, 255, 0.28);
    font-weight: 600;
    margin-bottom: 0.625rem;
  }

  .ai-cs-block-text {
    font-size: 0.9rem;
    color: rgba(240, 244, 255, 0.65);
    line-height: 1.7;
  }

  /* ─── SECTION 5: Architecture (cobalt-deep) ─── */
  .ai-arch {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0;
  }

  .ai-arch-node {
    width: 100%;
    max-width: 32rem;
    padding: 1.25rem 1.5rem;
    border-radius: 6px;
  }

  .ai-arch-node--clm {
    border: 1px solid rgba(255, 255, 255, 0.28);
    background: rgba(255, 255, 255, 0.06);
    text-align: center;
  }

  .ai-arch-node--agent {
    border: 1px solid rgba(255, 255, 255, 0.14);
    background: rgba(255, 255, 255, 0.04);
    flex: 1;
  }

  .ai-arch-node-label {
    font-family: ui-monospace, "Courier New", monospace;
    font-size: 0.8rem;
    letter-spacing: 0.04em;
    color: rgba(255, 255, 255, 0.92);
    font-weight: 600;
    margin-bottom: 0.25rem;
  }

  .ai-arch-node-desc {
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: rgba(255, 255, 255, 0.40);
    font-weight: 600;
    margin-bottom: 0.75rem;
  }

  .ai-arch-node-detail {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.60);
    line-height: 1.65;
  }

  .ai-arch-arrow-down {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.375rem;
    padding: 0.75rem 0;
  }

  .ai-arch-arrow-line {
    width: 1px;
    height: 2rem;
    background: rgba(255, 255, 255, 0.20);
  }

  .ai-arch-arrow-label {
    font-size: 9.5px;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: rgba(255, 255, 255, 0.32);
    font-weight: 600;
    text-align: center;
  }

  .ai-arch-arrow-label--up {
    color: rgba(255, 255, 255, 0.20);
  }

  .ai-arch-row {
    display: flex;
    gap: 1.5rem;
    width: 100%;
    max-width: 40rem;
    align-items: stretch;
  }

  .ai-arch-connector {
    width: 1.5rem;
    flex-shrink: 0;
    position: relative;
  }

  .ai-arch-connector::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 0;
    right: 0;
    height: 1px;
    background: rgba(255, 255, 255, 0.15);
  }

  .ai-arch-note {
    width: 100%;
    max-width: 40rem;
    border-top: 1px solid rgba(255, 255, 255, 0.10);
    padding-top: 1.25rem;
  }

  /* Learning loop */
  .ai-loop-steps {
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .ai-loop-step {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    flex: 1;
    min-width: 8rem;
    padding: 1rem;
    border: 1px solid rgba(255, 255, 255, 0.10);
    border-radius: 4px;
    background: rgba(255, 255, 255, 0.04);
  }

  .ai-loop-step-num {
    font-size: 10px;
    font-weight: 700;
    color: var(--cobalt-light);
    letter-spacing: 0.06em;
  }

  .ai-loop-step-text {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.65);
    line-height: 1.6;
  }

  .ai-loop-arrow {
    color: rgba(255, 255, 255, 0.25);
    font-size: 1.25rem;
    padding-top: 1.25rem;
    flex-shrink: 0;
  }

  /* ─── SECTION 6: Philosophy (cream) — compare table ─── */
  .ai-compare-table {
    border-collapse: collapse;
    min-width: 32rem;
  }

  .ai-compare-th {
    text-align: left;
    font-size: 9.5px;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    font-weight: 600;
    color: rgba(17, 24, 39, 0.38);
    padding: 0.75rem 1rem;
    border-bottom: 1px solid var(--cream-border);
    white-space: nowrap;
  }

  .ai-compare-th--dim {
    color: rgba(17, 24, 39, 0.22);
  }

  .ai-compare-th--highlight {
    color: var(--cobalt);
  }

  .ai-compare-row:nth-child(odd) td {
    background: rgba(17, 24, 39, 0.03);
  }

  .ai-compare-td {
    padding: 0.75rem 1rem;
    font-size: 0.875rem;
    color: var(--cream-sub);
    border-bottom: 1px solid rgba(17, 24, 39, 0.06);
    line-height: 1.5;
  }

  .ai-compare-td--dim {
    color: rgba(17, 24, 39, 0.45);
    font-weight: 500;
    font-size: 0.8125rem;
  }

  .ai-compare-td--highlight {
    color: var(--cobalt);
    font-weight: 500;
  }

  .ai-principle {
    color: var(--cobalt);
    padding-top: 2rem;
    border-top: 1px solid var(--cream-border);
  }

  /* ─── SECTION 7: Services (cobalt) ─── */
  .ai-service {
    padding: 1.5rem;
    border: 1px solid rgba(255, 255, 255, 0.14);
    border-radius: 4px;
    background: rgba(255, 255, 255, 0.06);
  }

  .ai-service-title {
    font-family: var(--font-serif);
    font-size: 1rem;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.95);
    letter-spacing: -0.01em;
    margin-bottom: 0.625rem;
  }

  .ai-service-body {
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.65);
    line-height: 1.7;
  }

  /* ─── SECTION 8: Related (navy) ─── */
  .ai-related-link {
    display: grid;
    grid-template-columns: 1.5rem 1fr;
    grid-template-rows: auto auto;
    column-gap: 0.75rem;
    row-gap: 0.25rem;
    padding: 0.875rem 0;
    border-bottom: 1px solid rgba(240, 244, 255, 0.06);
    text-decoration: none;
  }

  .ai-related-link:last-child {
    border-bottom: none;
  }

  .ai-related-arrow {
    grid-row: 1 / -1;
    align-self: center;
    font-size: 0.875rem;
    transition: color 0.15s;
  }

  .ai-related-text {
    font-family: var(--font-serif);
    font-size: 1rem;
    font-weight: 600;
    color: rgba(240, 244, 255, 0.80);
    letter-spacing: -0.01em;
    transition: color 0.15s;
  }

  .ai-related-link:hover .ai-related-text {
    color: #f0f4ff;
  }

  .ai-related-sub {
    font-size: 0.8125rem;
    color: rgba(240, 244, 255, 0.35);
    line-height: 1.5;
  }

  /* ─── Responsive ─── */
  @media (max-width: 640px) {
    .ai-level {
      grid-template-columns: 1fr;
      gap: 0.75rem;
    }

    .ai-level-marker {
      display: flex;
      gap: 0.75rem;
      align-items: baseline;
    }

    .ai-level-4 {
      margin-left: 0;
    }

    .ai-chain-row {
      grid-template-columns: 1fr;
      gap: 0.5rem;
    }

    .ai-cs-grid {
      grid-template-columns: 1fr;
      padding-left: 0;
    }

    .ai-cs-block--full {
      grid-column: auto;
    }

    .ai-arch-row {
      flex-direction: column;
    }

    .ai-arch-connector {
      width: auto;
      height: 1.5rem;
    }

    .ai-arch-connector::before {
      top: 0;
      bottom: 0;
      left: 50%;
      width: 1px;
      height: auto;
    }

    .ai-loop-steps {
      flex-direction: column;
    }

    .ai-loop-arrow {
      transform: rotate(90deg);
      padding: 0;
      align-self: center;
    }
  }
</style>
